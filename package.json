{
  "name": "lighting-lookup-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --import ./tests/support/register-loader.mjs --test $(find tests -name \"*.test.mjs\")"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "@upstash/redis": "^1.34.0",
    "@vercel/blob": "^2.6.1"
  }
}
